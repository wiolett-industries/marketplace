{
  "mcpServers": {
    "agent-memory": {
      "command": "npx",
      "args": [
        "-y",
        "@wiolett/agent-memory@latest"
      ],
      "note": "Agent Memory MCP. Registers persistent global/project memory tools (list/query/recall/save/update/inspect). Configure model access with npx -y @wiolett/agent-memory@latest init or OPENAI_API_KEY."
    }
  }
}
