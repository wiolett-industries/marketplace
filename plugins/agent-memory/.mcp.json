{
  "mcpServers": {
    "agent-memory": {
      "command": "npx",
      "args": [
        "-y",
        "@wiolett/agent-memory-mcp@latest"
      ],
      "note": "Agent Memory MCP. Registers persistent global/project memory tools. Configure model access with npx -y @wiolett/agent-memory-mcp@latest init or OPENAI_API_KEY."
    }
  }
}
