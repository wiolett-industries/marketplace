# Ask Questions

Standalone Codex plugin for clarification discipline.

This plugin ships one skill:

- `Ask Questions` for ambiguous requests where guessing wrong would change scope, behavior, constraints, or risk

The purpose is simple:

- ask the minimum useful questions
- avoid blocking on facts that can be checked quickly
- do not commit to the wrong direction when the request is still materially ambiguous
