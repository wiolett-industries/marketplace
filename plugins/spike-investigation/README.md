# Spike Investigation

Bounded feasibility spike plugin for Codex.

This plugin is for uncertain work where the right next step is not "pick an architecture and hope." Instead, it runs a few small experiments, compares the results, and turns that into a recommendation.

It ships:

- `Spike Investigation`

Use it when implementation uncertainty is real enough that a short throwaway experiment will save time.
