{
  "mcpServers": {
    "workflow": {
      "command": "npx",
      "args": [
        "-y",
        "@wiolett/workflow@latest"
      ],
      "note": "Workflow MCP. Registers deterministic .workflow/ artifact tools (status/create/update/complete/artifact/findings/handoff)."
    }
  }
}
