# UI Contract Review

Frontend design-contract and review plugin for Codex.

This plugin helps Codex do two things better:

- lock a practical UI contract before building
- review the implemented result after the build

It ships:

- `Using UI Contract Review`

Use it for frontend work where visual consistency, copy quality, spacing, and interaction behavior matter, both before and after implementation.
